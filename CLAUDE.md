After any content change, run `./build-zip.sh` and commit the updated zips.

Manifest of manifests: `~/software/aurasys-memory/memory/archive/reference-landmarks.md` — consult FIRST before multi-file plans.

Structure: `en/` = English, `es/` = Spanish, root `index.html` = language picker.
Each language is a self-contained directory with its own index.html and tutorials.
